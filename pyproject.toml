[project]
name = "response-review-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.5.9",
    "fastparquet>=2026.5.0",
    "matplotlib>=3.11.1",
    "pandas>=3.0.3",
    "pyyaml>=6.0.3",
    "sentence-transformers>=5.6.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
]
